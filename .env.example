# Data source: 0=yfinance only, 1=defeatbeta (disabled in code, always yfinance)
DEFEATBETA_ENABLED=0

# API keys (optional — app falls back to static/estimated data if unset)
FMP_API_KEY=your_fmp_api_key_here
FRED_API_KEY=your_fred_api_key_here
